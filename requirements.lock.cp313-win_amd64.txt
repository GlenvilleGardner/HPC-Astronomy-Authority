# HPC Astronomy Authority - RESOLVED RUNTIME ENVIRONMENT.
#
# The complete installed dependency closure under which A0.1 was established
# and verified. Captured from pip freeze in the repository .venv.
#
# PLATFORM-SPECIFIC. This lock is valid ONLY for:
#     CPython 3.13.7 on Windows AMD64 (cp313, win-amd64)
#
# numpy, pydantic_core and sgp4 ship platform-specific wheels
# (cp313-cp313-win_amd64). This file therefore does NOT describe, and must
# not be used to provision, a Linux environment.
#
# SCOPE OF THE CLAIM
#
# This file records a pinned, auditable Windows reference environment. It
# does NOT certify a clean-room reconstructed runtime, and it does NOT
# certify Linux/EC2 reconstruction. Linux dependency and runtime
# certification is deferred to EC2 bring-up, which will produce a sibling
# lock for that platform and re-run the golden vectors there.
#
# BOOTSTRAP TOOLCHAIN (environment construction, not a runtime dependency):
#     pip==25.2
# pip is recorded here as metadata only. It is deliberately absent from the
# requirement lines below and from scientificEnvironmentId.
#
# A0.1 reference point: this environment yielded
#     scientificEnvironmentId =
#     sha256:c223962508d19a9c969a9d62508497f6d0a418244c06509116668267e250fde0
#
# No hashes. Wheel digests here would pin win_amd64 artifacts and would fail
# outright on Linux; cross-platform hashes cannot be honestly produced before
# the Linux environment exists.

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
certifi==2026.2.25
click==8.3.1
colorama==0.4.6
fastapi==0.135.1
h11==0.16.0
idna==3.11
jplephem==2.24
numpy==2.4.3
pydantic==2.12.5
pydantic_core==2.41.5
sgp4==2.25
skyfield==1.54
starlette==0.52.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.41.0
